"use strict";
function combine(arr) {
    return arr[0];
}
const elem = combine([1, 2, 3, 4]);
const elem1 = combine(["one", "two", "three"]);
// elem1.toUpperCase()
console.log(elem);
console.log(elem1.toUpperCase());
